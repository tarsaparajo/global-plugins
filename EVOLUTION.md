# Evolution Ledger

Append-only record of every mirrored evolution of this plugin. Newest first. Each entry links a version to its change set, its parity proof, and any migration.

## [2.0.0] - 2026-06-22 — Clean bundle-member names (`_engine`→`engine`, `skills/_knowledge`→`_<slug>/knowledge`) + provenance stamp & version-gated migrations

- **Change set:** Two non-standard folders broke the plugin's own namespacing convention. (1) Reference doctrine was copied verbatim, un-prefixed, into the SHARED `skills/_knowledge/` dir, so two plugins co-installed in one `~/.codex/`/`~/.config/opencode/` collided on it — the exact failure the namespacing rule prevents. Doctrine is not a capability: it moved to a top-level canonical `knowledge/` folder and the generic classifier (`helpers.classifyTopLevelDir`) namespaces it to `_<slug>/knowledge/` on Codex/OpenCode (repo root on Claude via whole-repo). The `_knowledge` exception in `_base.ownerPrefixedCopy`/`collectComponents` and the `compliance.js` index-skip were removed; G5 (`executor.rewriteNamespacedRefs`) rewrites capability bodies that read `knowledge/…` to `_<slug>/knowledge/…` per provider. (2) The runtime payload member `_engine` carried a redundant underscore (the bundle `_<slug>/` already marks infra); members now carry clean names. `helpers.payloadBasePath` (`'_engine'`→`'engine'`), `BUNDLE_SIBLING_NAMES`/`RESERVED_DIR_NAMES`, `build-opencode.js` (offset `../../engine` + payloadRoot), `compliance.js` (bundleLabel), `codex-home`/`opencode-home` comments, the migration runner, and all doctrine/agent/skill/docs bodies track the new name. (3) NEW provenance + gating: `scripts/evolve/project.mjs` writes `install-state.json` per provider with `schemaVersion` + `generatedWith` (children seed `.evolution/baseline/provenance.json`); `scripts/evolve/migrate-apply.mjs` now SemVer-gates the chain (only migrations whose `from` contains the installed version run; clean apply re-stamps to HEAD) and gained three structured step kinds (`relocate-subpath`, `rename-bundle-member`, `rebuild-opencode-dist`). Doctrine: `knowledge/namespacing.md` rewritten (clean member names, `../../engine` invariant, reserved-name guard); `migrate` skill (parent + child) describes the stamp-based pending chain; `generate`/`adapt` seed the provenance stamp. Tests: `test_projection_drift.js`, `test_all_providers.js`, `test_migrate_apply.js`, `_fixture.js` track the new layout; the migration was verified end-to-end (dry-run/apply/rollback/idempotence) against a synthetic old-layout install.
- **Why it propagates to children:** the classifier, the stamp writer (`project.mjs`), the gated runner (`migrate-apply.mjs`), and the new step kinds live in the engine + `scripts/evolve/`, which every child seeds in its own root and carries as its `_<slug>/engine/` payload; the doctrine lives in `knowledge/` (namespaced into the bundle on Codex/OpenCode, repo root on Claude); the `generate`/`adapt` doctrine seeds the provenance stamp; and `migrations/2.0.0.md` is mirrored into `templates/child/governance/migrations/2.0.0.md`. So a child generated from any provider is born with clean member names AND carries the 2.0.0 migration; an already-materialized child re-seeds the migration on its next `/evolve` and brings its own install forward with `/migrate`.
- **Self-application:** global-plugins applies the rule to itself — its one invented folder `knowledge/` now namespaces to `_global-plugins/knowledge/` on Codex/OpenCode (repo root on Claude), and its payload member is `_global-plugins/engine/`. The committed projections were regenerated (`project.mjs --apply` + `build-opencode.js`); the OpenCode `dist/tools` resolves `../../engine`.
- **Parity proof:** `node tests/run-all.js` → 78/78 after re-projection (drift tests #4–#7 updated to the new layout; provider + migrate unit tests pass). `npm run validate` → `{ ok: true, findings: [] }`. `project.mjs --apply` idempotent (byte-equality drift check passes on all three providers). Migration 2.0.0 verified end-to-end on a synthetic `demo` install: dry-run plans the two relocations + dist rebuild; apply moves `skills/_knowledge`→`_demo/knowledge`, `_demo/_engine`→`_demo/engine`, rebuilds dist with the `../../engine` offset; re-apply is a no-op for the moves; rollback restores the old layout. SemVer gating unit-tested (range `>=1.1.0 <2.0.0` includes 1.1.0/1.2.0, excludes 2.0.0; pre-stamp → whole chain).
- **Migration:** [migrations/2.0.0.md](migrations/2.0.0.md) — BREAKING for Codex/OpenCode installs with a loose `skills/_knowledge/` and/or `_<slug>/_engine/`; Claude unaffected (whole-repo); fresh 2.0.0 installs need none. Gated `from: >=1.1.0 <2.0.0`. Pre-1.x parity: MAJOR (folder placement + bundle-member name changed for installed copies).

## [1.2.0] - 2026-06-21 — Hero skeleton: a shared, agent-authored SVG model + zero-dependency conversion, propagated to every child

- **Change set:** The hero was produced only by a deterministic Node generator (`assets/build-hero.js`, and its parametrized twin `templates/child/assets/build-hero.js` seeded into children), so a child's banner was script-rendered from metadata. This adds a *skeleton model* the children author from instead — the way global-plugins itself is a model for its children — without touching the renderer. NEW canonical files: `templates/child/assets/hero.skeleton.svg` (a neutral, self-contained, well-formed 2400×1350 SVG cloned structurally from the real hero, every plugin-specific value replaced by a `[PLACEHOLDER]`, no reference to any plugin, with section comments + `PALETTE:`/`LOGO:` knob markers); `templates/child/assets/hero-svg.md` (line-by-line annotation: each gradient/filter, the logo-mark anatomy, the card model, the footer, a field→region table); `templates/child/assets/hero-authoring.md` (the fill-in sequence: gather data → choose palette → choose logo → fill fields with the agents/skills/providers grouping rules → write SVG → convert to PNG); `skills/_knowledge/hero-skeleton.md` (first-class doctrine: the model authored-not-rendered for children; palette + logo + copy are the ONLY per-plugin variation; a logo gallery — `share` default, `nodes`, `hexagon`, `spark` + a raw `svgPath`/inline-SVG escape hatch, all palette-colored in the 100×100 mark space; the tiered zero-dependency SVG→PNG methodology `sharp → @resvg/resvg-js → headless Chrome → manual`, always writes the SVG first, never hard-fails, no hard dependency). DOCTRINE wiring: `skills/generate/SKILL.md` (steps 4 + 7 — seed the skeleton ecosystem, author a bespoke `hero.svg`, convert to PNG) and `skills/adapt/SKILL.md` (step 5 — a hero-less plugin gains one authored from the skeleton; an existing hero is preserved); `skills/_knowledge/readme-skeleton.md` §7 now points to the dedicated doctrine; `agents/plugin-architect.md` + `agents/capability-extractor.md` describe the hero as a shared skeleton; both asset READMEs document the ecosystem + the palette/logo knobs + the conversion. The deterministic `build-hero.js` is UNCHANGED — it remains a convenience generator and the reference implementation of the conversion chain. Tests: 3 new drift assertions in `tests/test_projection_drift.js`.
- **Why it propagates to children:** the doctrine lives in `skills/_knowledge/` (projected verbatim to all three providers via `skills-core`); the skeleton SVG + the two guides live under `templates/child/assets/` and ride the `child-templates` payload channel verbatim into `.codex/_<slug>/_engine/templates/child/assets/` and the `.opencode` equivalent; the authoring is in the canonical `generate`/`adapt` doctrine. So a child generated/adapted from any provider carries the skeleton + guides + conversion methodology from birth and authors its own hero (its own palette + logo) to the same standard. No engine-shape change — the anti-bloat guard keeps README/docs/assets out of every provider's capability surface.
- **Self-application:** global-plugins' OWN hero is unchanged in mechanism (still rendered by its `assets/build-hero.js`); it gains the skeleton model + doctrine + guides for children. The parent's local `assets/` (in `SKIP_DIR_NAMES`) is never projected; only the version pill + 14 README badges bumped and the parent hero regenerated.
- **Parity proof:** `node tests/run-all.js` → 78/78 (the 3 new tests: hero doctrine projects into every provider `skills/_knowledge/`; skeleton + guides ride the `_engine/` payload on codex+opencode; skeleton is neutral + well-formed). `npm run validate` → `{ ok: true, findings: [] }` after re-projection. `project.mjs --apply` is idempotent (the committed-projection byte-equality drift check passes on all three providers). Verified the dry-run plan adds exactly 9 NEW projection ops (the doctrine to 3 `_knowledge/` dirs + the 3 template assets to both `_engine/` payloads) and `manifests/modules.json` is unchanged. The skeleton was confirmed well-formed (xmllint) and rasterizes via headless Chrome. Hero pill → `v1.2.0`.
- **Migration:** none — additive doctrine + templates; the renderer and projection topology are unchanged. Previously generated children gain the skeleton ecosystem on their next `/evolve` re-author. Pre-1.x parity: MINOR, no breaking change.

## [1.1.0] - 2026-06-21 — Generic non-standard-folder namespacing: ANY invented folder → `_<slug>/<folder>/`, computed per provider

- **Change set:** 1.0.0 namespaced only three KNOWN artifacts (`_engine/`, `dist/`, `install-state.json`); any OTHER folder a plugin invented was silently dropped by the anti-bloat guard or left loose at the provider root. This generalizes the rule to a COMPUTED classification. Engine: `helpers.classifyTopLevelDir` (single source of truth: SHARED→root, PRIVATE→`_<slug>/<folder>/`, SKIP→drop-with-note, unknown→PRIVATE never DROP) + `helpers.bundleSubPath` (placement primitive; `payloadBasePath` re-expressed through it, byte-identical) + `SKIP_DIR_NAMES`/`BUNDLE_SIBLING_NAMES`; `_base.claimedTopLevel` (subtracts every module-declared dir — applicability ≠ placement, R7) + `_base.namespacePrivateFolders` (scans the canonical root, emits bundle ops for PRIVATE, root-copy ops for pinned-invented, warnings for every move/skip); `codex-home`/`opencode-home` wire the scanner and stamp `namespacedFolders` on capability ops; `executor.rewriteNamespacedRefs` (G5: rewrites `<folder>/…` → `_<slug>/<folder>/…` for codex/opencode, idempotent negative-lookahead; claude never stamped); `registry.json` per-provider `pinnedToRoot` + `wholeRepoInstall` frozen onto the adapter (R2); `planScaffold`/`projector.collectWarnings`/`project.mjs --dry-run` surface the re-home warnings at the human-gate (G3). Runner: `scripts/evolve/migrate-apply.mjs` rewritten from a pure reporter into a real interpreter (`relocate-nonstandard-dirs` via the SAME classifier + `remove-path`, snapshot forward/verify/rollback, dry-run, idempotence); `migrations/1.0.0.md` re-expressed to the structured schema; new `migrations/1.1.0.md`; `migration.md.tmpl` + child schema/runner updated. Doctrine: `namespacing.md` (computed PRIVATE/SHARED/SKIP rule, per-provider pinned-to-root, G5 convention), `provider-matrix.md` (pinned-to-root table + reverse re-home), `plugin-architect` (`nonStandardFolders[]`), `capability-extractor`/`adapt` (inventory-subtract-warn), `migration-analyzer` (folder-placement breaking criterion + structured steps), `canonical-projector`/`generate`.
- **Why it propagates to children:** the classifier + scanner + rewrite live in the engine (`helpers`/`_base`/`executor`), which every child seeds in its own root and carries as its `_engine/` payload; the doctrine lives in `skills/_knowledge/` (projected to all three providers); the migration runner + structured-step schema are in the child template. So a child generated/adapted from any provider namespaces its invented folders from birth — and a child that ships a folder + a body that reads it gets the per-provider reference rewrite automatically.
- **Self-application:** global-plugins' OWN layout is UNCHANGED — every one of its top-level folders is module-declared (engine/manifests/templates ship as payload; docs is claude-only) or dev-meta (`tests/`/`migrations/`/`assets/` → SKIP), so the classifier is a no-op for it. Self-application = the engine GAINS the capability + doctrine, not a layout churn. Verified: `migrate-apply.mjs --dry-run .` and `project.mjs --dry-run` plan ZERO non-standard re-homes for global-plugins.
- **Parity proof:** `node tests/run-all.js` — 12 NEW tests (generic folder → bundle on codex/opencode; pinned-to-root per provider; G5 reference rewrite + idempotence + boundary; migration dry-run/apply/idempotence/rollback; opencode round-trip byte-identical) plus the existing suite; `npm run validate` → `{ ok: true }` after re-projection; re-project idempotent.
- **Migration:** [migrations/1.1.0.md](migrations/1.1.0.md) — BREAKING for codex/opencode installs that carry a loose non-standard folder (its placement moves into `_<slug>/<folder>/`); claude unaffected; fresh installs need no migration. Relocates generically via the classifier (no fixed list); dry-run/apply/rollback via `/global-plugins:migrate`.

## [1.0.0] - 2026-06-21 — Per-plugin private bundle `_<slug>/`: non-standard folders namespaced so installs never collide

- **Change set:** Non-standard infrastructure (the `_engine/` runtime payload, the OpenCode `dist/` compiled plugin, the install-state file) was loose at the OpenCode/Codex config root with no namespacing, so installing a second plugin overwrote the first's `_engine/`/`dist/`. Now all of it groups under a per-plugin private bundle `_<slug>/`, with the OpenCode discovery loader at the shared `plugins/<slug>.js` and slug-prefixed tool names. Engine: `helpers.privateBundleDir`/`payloadBasePath` (single source of truth for the layout) + reserved-slug guard + `RESERVED_DIR_NAMES`; `_base.payloadCopy` ships into `_<slug>/_engine/`; `build-opencode.js` writes `_<slug>/dist/`, emits `plugins/<slug>.js`, slug-prefixes tool names, and **fixes the latent payload-resolution bug** (`dist/tools` now resolves `../../_engine`, a real sibling, instead of the never-existent `dist/_engine`); `opencode-home`/`codex-home` thread the slug; `registry.getInstallStatePath` + `adapters/registry.json` use a `${bundle}` token; `parity.checkNoOrphan` excludes build-step outputs; `compliance.checkProjectionDrift` checks the namespaced payload + the reserved-slug guard. Doctrine: new `skills/_knowledge/namespacing.md` (PRIVATE vs SHARED classification, the dist↔_engine sibling invariant, OpenCode discovery contract, Codex's ignore-unknown-dir behavior, reserved-slug guard, self-application); `generate`/`adapt` SKILLs, `canonical-projector`/`plugin-architect` agents, `provider-matrix`/`opencode-harness`/`codex-harness` references, and the README all teach it.
- **Why it propagates to children:** the layout is computed by the engine (`helpers`/`_base`/`build-opencode`), which every child seeds in its own root and carries as its `_engine/` payload; the doctrine lives in `skills/_knowledge/` (projected to all three providers). So a child generated/adapted from any provider produces the namespaced layout from birth — no child migration needed (a fresh child starts at 0.1.0 with the bundle, never the flat layout).
- **Parity proof:** `node tests/run-all.js` — provider suite incl. a NEW two-plugins-side-by-side non-collision test + per-provider namespacing tests; `npm run validate` → `{ ok: true, findings: [] }` after re-projection. Re-project is idempotent. Verified: payload at `_global-plugins/_engine/`, dist at `_global-plugins/dist/`, loader at `plugins/global-plugins.js`, standard dirs untouched at the root, NO flat `_engine`/`dist` left, and `dist/tools` resolves `../../_engine` to the real sibling.
- **Migration:** [migrations/1.0.0.md](migrations/1.0.0.md) — BREAKING for already-installed codex/opencode copies (flat `_engine/`/`dist/` become orphans on re-copy). Removes the flat dirs + old `global-plugins-install-state.json`, then re-copies the namespaced layout; dry-run/apply/rollback via `/global-plugins:migrate`. Claude unaffected; fresh installs need no migration.

## [0.10.0] - 2026-06-21 — Child README skeleton standard: hero + badges + full i18n + seeded hero generator

- **Change set:** Children previously inherited a bare README (`templates/governance/README.md.tmpl`: switcher + title + body slots) — no centered block, no hero, no badges, and no doctrine that every locale README must carry them. Added `skills/_knowledge/readme-skeleton.md` (the decontextualized standard: centered `<div align="center">` block above `## Overview`, hero in every locale, dual switcher from `config/locales.json`, three badges with `version == VERSION`, per-provider Install boxes for resolved targets only, and a rules-install box when a rules layer exists). Upgraded `templates/governance/README.md.tmpl` with the centered block + `![{{plugin.name}}](assets/hero.png)` + a new `{{plugin.badges}}` slot. Added a seeded, parametrized hero generator `templates/child/assets/build-hero.js` (+ `assets/README.md`): it auto-derives metadata from the child, **derives the gradient palette deterministically from the plugin name** so each child has its own color identity (overridable via `assets/hero.config.json`), always writes `hero.svg`, and best-effort rasterizes `hero.png` via `sharp`/`@resvg/resvg-js` with a manual fallback (zero hard deps, never hard-fails). Wired `skills/generate` (steps 4 + 7) and `skills/adapt` (new README step) to author the root + all 13 locale READMEs to the standard and seed/run the generator; updated `agents/plugin-architect` (Communication dimension) and `agents/capability-extractor` (lift README/docs/assets) to treat README/docs/assets as governance artifacts. Also fixed the parent's stale `version-0.7.0` README badges (root + 13 locales) → `version-0.10.0`, and the hero pill → `v0.10.0`.
- **Why it propagates to children:** the standard lives in `skills/_knowledge/` (projected to all three providers, copied verbatim with no prompt-defense injection); the child README template + hero generator live under `templates/` and ride the `child-templates` payload channel verbatim into `.codex/_engine/templates/` and `.opencode/_engine/templates/`; the authoring is in the canonical `generate`/`adapt` doctrine. No engine-shape change — the anti-bloat guard keeps README/docs/assets out of every provider's capability surface.
- **Parity proof:** `node tests/run-all.js` → 60/60; `npm run validate` → `{ ok: true, findings: [] }`. `project.mjs --apply` is idempotent (re-run plans no new ops). Verified the dry-run plan writes the new `_knowledge/readme-skeleton.md` into all three dotfolders and the new `templates/child/assets/*` + upgraded `README.md.tmpl` into both `_engine/` payloads, and that **zero** ops touch any dotfolder's `docs/` or `assets/`. The seeded generator was exercised on two child names and produced distinct, deterministic palettes. (The `verify-parity.mjs noOrphan` check flags `.opencode/dist/*` build artifacts; this is pre-existing on clean HEAD and unrelated to this change — `npm run validate` is the authoritative gate.) Hero pill → `v0.10.0`.
- **Migration:** none — additive doctrine/template change; previously generated children gain the skeleton on their next `/evolve` re-author. Pre-1.0 MINOR, no breaking change.

## [0.9.0] - 2026-06-21 — Runtime-payload projection: installed plugins self-generate on every provider

- **Change set:** Generation needs the projection engine present on disk; only Claude's whole-repo install carried it, so Codex/OpenCode installs (and even generated children) could not generate/adapt/evolve. Added a NEW, additive runtime-payload channel that ships `engine/ + scripts/evolve/ + manifests/ + adapters/ + templates/ + .evolution/baseline/` into a reserved non-capability subdir `_engine/` of Codex/OpenCode installs — without touching the 0.7.0 capability guard. Mechanism: `payloadTargets[]` on `engine-core`/`manifests-core`/`child-templates` (`manifests/modules.json`); a `payloadCopy` verbatim handler + `RESERVED_PAYLOAD_DIR` in `engine/providers/_base.js` (fixed the stale fallback-copy comment); `allModules` threaded through `registry.planScaffold`/`planOperations`; codex-home/opencode-home opt in; the executor skips frontmatter-adapt + prompt-defense for `verbatim` payload ops so it stays byte-identical to canonical. OpenCode `dist/` is now a real native plugin (`engine/build-opencode.js`) exposing generate/adapt/evolve/validate/migrate tools that delegate to `_engine/`. The generate/adapt doctrine now seeds the engine into every child (closing the latent bug where child re-projection scripts required `./engine` that was never copied). Drift guard + `compliance.js` extended to byte-check the payload, forbid capability-surface leaks, and assert payload completeness.
- **Why it propagates to children:** the seeding is in the canonical generate/adapt doctrine; the payload channel is engine-level; both flow to children through the normal projection + authoring path.
- **Parity proof:** `node tests/run-all.js` → 60/60 (added the payload-completeness drift test); `npm run validate` → `{ ok: true, findings: [] }`. Re-project is idempotent (no new diff on second apply); the `_engine/` payload is ~304 KB per provider and contains no nested dotfolder (no recursion). The engine was PROVEN portable: copied `.codex/_engine/` to a temp dir, ran `node scripts/evolve/project.mjs --dry-run` from there, and it planned 127 ops across claude/codex/opencode with zero dependency on the original repo. Hero pill → `v0.9.0`.
- **Migration:** none as a data migration. Existing Codex/OpenCode installs gain the capability by re-copying the updated `.codex/.`/`.opencode/.` (the `_engine/` subdir rides along via the same `cp -R .codex/. ~/.codex/` merge). Capability surface, Claude output, and all prior invariants unchanged.

## [0.8.1] - 2026-06-21 — Codex install merge-copy fix + child README install section

- **Change set:** (1) The Codex install instruction `cp -r .codex ~/.codex` nested the dotfolder inside an existing `~/.codex/` (→ `~/.codex/.codex/…`), so Codex never found its config. Fixed to `mkdir -p ~/.codex && cp -R .codex/. ~/.codex/` (trailing `/.` merges contents) across `README.md` + all 13 locale READMEs; aligned OpenCode's `cp -r`→`cp -R`; corrected stale `agents/*.toml` prose → `[agents.<name>]` tables in `config.toml`. (2) Generated children previously had NO install instructions — added an `## Installation` section (`{{plugin.install}}` slot) to `templates/governance/README.md.tmpl` and taught `skills/generate/SKILL.md` step 7 to compose it from the resolved targets using the safe merge-copy form. `adapters/providers/codex-home.md` now documents the install command. Documentation/template/doctrine only — no engine or projection-shape change.
- **Why it propagates to children:** the install section lives in the child README template + the canonical `generate` doctrine, both consumed when a child is generated/projected.
- **Parity proof:** `node tests/run-all.js` → 59/59; `npm run validate` → `{ ok: true, findings: [] }`. The canonical `generate` skill edit re-projected to all three providers (drift guard green). Simulated the corrected command into a pre-existing dir: `cp -R .codex/. /tmp/.../.codex/` yields `config.toml` at the root with NO nested `.codex/.codex` (the old `cp -r` form reproduced the nesting). Child README render shows correct per-provider merge-copy commands. Hero pill bumped to `v0.8.1`.
- **Migration:** none — corrects install documentation; no installed artifact or canonical contract changes. Users who previously hit the nesting bug should remove the stray `~/.codex/.codex/` and re-run the corrected command.

## [0.8.0] - 2026-06-21 — OpenCode colors via quoted hex + plugin-owner identity for OpenCode/Codex

- **Change set:** Two user-facing improvements, both engine-level so generated children inherit them. (1) OpenCode agent `color` is re-enabled: the named→hex rewrite already existed but emitted a BARE hex (`color: #06B6D4`), which YAML reads as a comment (empty value) → OpenCode rejected the agent. `engine/frontmatter.js` `needsYamlQuoting` now quotes any `#`-leading scalar, so colors emit as `color: "#06B6D4"`; theme tokens stay bare; the drift guard + `engine/compliance.js` now require quoted hex. (2) Owner-identity for the two CLIs without native namespacing (Claude already has `/plugin:cmd`): a `[<plugin>] ` description prefix on every OpenCode/Codex skill/command/agent (frontmatter + Codex `[agents.<name>]` tables + the `AGENTS.md` index), an OpenCode filename/name owner-prefix `<plugin>-<name>` on command/agent files and skill dir+`name:` (so `/<plugin>-<cmd>`), and a `### <plugin>` owner-grouping of the Codex `AGENTS.md` index. New helpers `pluginLabel`/`prefixDescription`/`prefixName`/`stripOwnershipPrefix` + `ownerPrefixedCopy`; a `decodeYamlScalar` (and an unescaping `readFrontmatter`) so a prefixed-then-requoted description never nests escapes. Slug source: `.claude-plugin/plugin.json` → `package.json` `name`. Doctrine + child-generation agents updated; reverse-strip rules added to `capability-extractor`.
- **Why it propagates to children:** all logic lives in `engine/` (`frontmatter.js`, `helpers.js`, `builder.js`, `providers/_base.js`, `providers/opencode-home.js`, `executor.js`); children ship a copy of `engine/` and re-project through it.
- **Parity proof:** `node tests/run-all.js` → 59/59 (added color-quoting, `[plugin]`-prefix idempotency, owner-rename, Codex-grouping, and a `decodeYamlScalar` round-trip); `npm run validate` → `{ ok: true, findings: [] }`. All committed `.claude`/`.codex`/`.opencode` regenerated via `project.mjs --apply` (idempotent; drift guard byte-matches; OpenCode capability files recorded as git RENAMES, `_knowledge` unprefixed). Strict PyYAML/tomllib validate all 49 projected frontmatter blocks + `config.toml`; the OpenCode hero render confirms the `v0.8.0` pill.
- **Migration:** none as a data migration, but NOTE for already-installed OpenCode copies: invocation strings change (`/adapt` → `/<plugin>-adapt`) on re-projection — a deliberate usability change, not a break of the canonical contract. Claude output is unchanged (named colors kept, no prefix, no rename).

## [0.7.1] - 2026-06-20 — Codex install correctness: baseline TOML table + quoted SKILL.md YAML

- **Change set:** Two Codex install defects, one evolution, both fixed in the shared engine so they propagate to generated children. (1) `engine/builder.js` `codex:config-toml` now emits the Prompt Defense Baseline under a dedicated `[prompt_defense]` table (`baseline = "…"` via `tomlString()`) instead of a bare root `prompt_defense_baseline = """…"""` key — a bare root key, when the generated config is merged after an existing `~/.codex/config.toml` ending in tables (e.g. `[desktop]`), binds to the last table; a table header is append-safe at any position, and the single-line escaped string also retires the brittle triple-quote form. (2) `engine/frontmatter.js` `serialize()` now double-quotes any scalar a YAML reader would misparse — specifically a `description` containing `: ` (the mapping-value trap that made Codex skip `validate`/etc. with `mapping values are not allowed in this context`) and surrounding whitespace — escaping inner `\`/`"`; the quoter is narrow so hex colors/theme-tokens/names/inline arrays stay byte-stable. The one offending canonical source (`skills/validate/SKILL.md`) was quoted at source so it is valid YAML for all three providers. Doctrine updated (`adapters/providers/codex-home.md`, `skills/_knowledge/codex-harness.md`, `skills/_knowledge/governance.md`); regression tests added (`tests/test_frontmatter.js` colon-quoting round-trip + `tests/providers/test_all_providers.js` `[prompt_defense]`-table + valid-SKILL-YAML assertions, fixture `builder` description given a colon).
- **Why it propagates to children:** both fixes are in `engine/builder.js` + `engine/frontmatter.js`, and children ship a copy of `engine/` and re-project through it (`scripts/evolve/project.mjs`) — no child template change needed.
- **Parity proof:** `node tests/run-all.js` → 54/54; `npm run validate` → `{ ok: true, findings: [] }`. All committed `.claude`/`.codex`/`.opencode` regenerated via `project.mjs --apply` (idempotent; drift guard byte-compares committed vs fresh). Strict PyYAML validation of all 56 projected frontmatter blocks passes (was 1 failure at `validate` line 3 col 223). Real-merge simulation: generated config appended after a `[desktop]` block parses with `tomllib` to `prompt_defense.baseline` at root, no `[desktop]` leak, 7 agent tables intact.
- **Migration:** none — corrects invalid/ fragile shipped Codex output; the canonical→provider contract is unchanged. Already-installed copies are corrected by re-projection, not a data migration.

## [0.7.0] - 2026-06-20 — Regenerate stale projections + permanent drift guard + projection-surface fix

- **Change set:** Three faults, one evolution. (1) Every committed projection was STALE — the engine had been correct since 0.5.1/0.6.0 (drop `model:`, rewrite named color → hex for OpenCode) but the committed `.claude`/`.opencode`/`.codex` files (14 agents + the `_knowledge` doctrine docs + `.codex/AGENTS.md`) were never regenerated, so OpenCode rejected `color: cyan` on install and the docs still described the old keep/keep rules. Regenerated all via `node scripts/evolve/project.mjs --apply`. (2) `engine/providers/_base.js` `planFromModules` verbatim-copied any unhandled canonical dir, leaking `engine/`/`adapters/`/`manifests/`/`config/`/`templates/`/`docs/` into every dotfolder; it now projects ONLY explicitly-handled dirs, and `engine/registry.js` `planScaffold` now honors each module's `targets[]` registry. (3) New permanent guard: `tests/test_projection_drift.js` byte-compares every committed projection against a fresh one (plus targeted `model:`/color/infra-leak checks) and `engine/compliance.js` `audit()` gains a `projection-drift` finding.
- **Why it recurred (and won't again):** nothing ever inspected the committed bytes. `engine/parity.js` only re-runs the planner in memory (structural equivalence — a stale on-disk file passes), and `audit()` had no projection check, so it returned `ok: true` over 14 broken files. The "re-projected" claims in the 0.5.1/0.6.0 ledger entries only ever touched live `~/.config` + the cache, never the repo's committed dotfolders. The new byte-equality test + compliance finding close that exact gap.
- **Parity proof:** `node tests/run-all.js` → 50/50; `npm run validate` → `{ ok: true, findings: [] }`. Re-running `project.mjs --apply` is IDEMPOTENT (no new git changes), proving committed output == engine output. Committed OpenCode agents carry only hex colors (e.g. `#06B6D4`); no `model:` in any projected agent; `.claude`/`.opencode`/`.codex` back to their intended surface (`agents`/`skills`/`commands` + OpenCode `dist/`).
- **Migration:** none — corrects invalid shipped output and removes wrongly-shipped infrastructure from dotfolders; the canonical→provider contract is unchanged and the model drop only widens user choice. Already-installed copies are corrected by re-projection, not a data migration.

## [0.6.0] - 2026-06-20 — Agents never preset a `model` (drop on every provider)

- **Change set:** `model` becomes a universal **DROP** in `engine/frontmatter.js` (was keep for claude / rewrite→`provider/model` for opencode / drop for codex) — a top-of-loop drop runs before any per-target branch, so no provider ever receives a preset model. The claude projection is now routed through the adapter too (`engine/providers/_base.js` `frontmatterTargetFor` no longer short-circuits claude) so a stray authored `model:` is stripped there as well; `adapt()` stays byte-stable for claude when no `model:` is present. Removed the `model:` line from all 7 canonical agents and both `templates/child/agents/*.md`. Added explicit "never preset model" instructions to `plugin-architect` + `canonical-projector` so generated children comply. `CLAUDE_TO_OPENCODE_MODEL` kept/exported for back-compat (now unused internally). Doctrine corrected across `provider-matrix.md`, `opencode-harness.md`, `claude-code-harness.md`, `adapters/providers/opencode-home.md`, and engine comments (`builder.js`, `executor.js`). Tests inverted to assert ABSENCE of `model:` on every projection, with a stray `model:` retained in fixtures to prove the drop.
- **Parity proof:** `node tests/run-all.js` → 44/44; `npm run validate` → `{ ok: true, findings: [] }`. All three projections verified free of any `model:` frontmatter; claude output byte-identical for model-less agents; live `~/.config/opencode`, `~/.claude`, `~/.codex`, and the 0.6.0 cache re-synced.
- **Migration:** none required for correctness — removing a preset model only widens user choice (the CLI selection now governs). Already-installed copies are corrected by re-projection / the live + cache rewrite below, not a data migration.

## [0.5.1] - 2026-06-20 — OpenCode `color` rewrite (named → hex)

- **Change set:** `engine/frontmatter.js` now rewrites a named Claude color → hex `#RRGGBB` for the OpenCode target (new `CLAUDE_TO_OPENCODE_COLOR` table + `OPENCODE_THEME_TOKENS` guard), changing `color` from a **keep** to a **rewrite** — an already-hex value or a valid theme token is kept, an unrecognized name is dropped. Root cause: OpenCode's `color` schema accepts only hex or 7 theme tokens, so the prior verbatim "keep" emitted invalid agents that OpenCode rejected on install (`got "cyan"`). Corrected the matching doctrine in `skills/_knowledge/provider-matrix.md`, `skills/_knowledge/opencode-harness.md`, and `adapters/providers/opencode-home.md`. Tests updated: `tests/test_frontmatter.js` (+3 color cases) and `tests/providers/test_all_providers.js` (opencode assertion).
- **Parity proof:** `node tests/run-all.js` → 44/44; `npm run validate` → `{ ok: true, findings: [] }`. All three dotfolders re-projected; OpenCode agents now carry hex `color` values (e.g. `#06B6D4`) that satisfy OpenCode's validator; `.codex` still free of any `color` frontmatter; Claude unchanged (named color kept verbatim — canonical IS Claude shape).
- **Migration:** none — non-breaking. The canonical→provider contract is unchanged; the OpenCode output was invalid before and is now valid. Already-installed copies are corrected by re-projection, not a data migration.

## [0.5.0] - 2026-06-20 — Frontmatter adaptation across providers

- **Change set:** new `engine/frontmatter.js` (keep/rewrite/drop) wired into the executor via a `frontmatterTarget` op tag (`engine/providers/_base.js`); Codex agents re-expressed as `config.toml` `[agents.<name>]` tables instead of invalid `.codex/agents/*.toml` (`engine/providers/codex-home.js`, `engine/builder.js`); new "Frontmatter field adaptation" doctrine in `skills/_knowledge/provider-matrix.md`; corrected Codex/OpenCode harness + adapter docs and the `capability-extractor` reverse-transform; the child-generation agents (`canonical-projector`, `plugin-architect`) now consult the matrix so children adapt frontmatter too.
- **Parity proof:** `npm run validate` → `{ ok: true, findings: [] }`; `node tests/run-all.js` → 41/41 (incl. per-provider projection + parity + compliance audit). All three dotfolders re-projected; `.codex` verified free of `color`/`model`/`tools` frontmatter leakage; OpenCode agents carry `tools` objects + `provider/model`; Claude unchanged (byte-identical canonical shape).
- **Migration:** none — no breaking change to the canonical→provider contract; only the Codex output shape was corrected (it was invalid before).

<!-- Entries are appended above this line by the evolve pipeline. -->
