# Assets

- `hero.svg` — the source vector for the README hero banner (2400×1350). Edit it via `build-hero.js`.
- `hero.png` — the rendered banner referenced at the top of the root `README.md`.
- `build-hero.js` — regenerates `hero.svg`. Run `node assets/build-hero.js`, then render the SVG to `hero.png`.
